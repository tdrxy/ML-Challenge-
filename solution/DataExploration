from sklearn.preprocessing import LabelEncoder

from challenge import get_data
import pandas as pd
pd.set_option('display.max_columns', 500)
pd.set_option('display.width', 1000)

X,y = get_data()
print(X.head(10))
print(X.isnull().sum())

X = X.dropna()
print(X.dtypes)
print(X.describe())

# Correlation education, education-num
# grouped_X = X[['education', 'education-num']].groupby(['education'])
# for key, item in grouped_X:
#     print(grouped_X.get_group(key), "\n\n")

